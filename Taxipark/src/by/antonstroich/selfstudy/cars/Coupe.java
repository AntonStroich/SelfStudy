package by.antonstroich.selfstudy.cars;

public class Coupe extends Car {

    public Coupe(String model, int number, int cost, int fuel) {
        setModelCar(model);
        setRegistrationNumber(number);
        setCarCost(cost);
        setFuelSpend(fuel);
        setSeatNumber(2);
    }
}
