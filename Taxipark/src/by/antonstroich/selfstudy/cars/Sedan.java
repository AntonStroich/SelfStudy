package by.antonstroich.selfstudy.cars;

public class Sedan extends Car {

    public Sedan(String model, int number, int cost, int fuel) {
        setModelCar(model);
        setRegistrationNumber(number);
        setCarCost(cost);
        setFuelSpend(fuel);
        setSeatNumber(4);
    }
}
